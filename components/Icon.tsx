import React from 'react';
import { 
  Check, X, Trophy, Star, Users, Home, Settings, LogOut, 
  Trash2, Plus, Calendar, Medal, Award, User, ChevronRight, Lock, ArrowLeft,
  Upload, Camera, KeyRound, Image as ImageIcon, Mail, Send, MessageCircle
} from 'lucide-react';

export const Icons = {
  Check, X, Trophy, Star, Users, Home, Settings, LogOut, 
  Trash2, Plus, Calendar, Medal, Award, User, ChevronRight, Lock, ArrowLeft,
  Upload, Camera, KeyRound, ImageIcon, Mail, Send, MessageCircle
};

export default Icons;